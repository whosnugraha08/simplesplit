/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      {
        protocol: 'https',
        hostname: '*.supabase.co',
        pathname: '/storage/v1/object/public/**',
      },
    ],
  },
  // Disable static page generation to avoid Supabase connection errors at build time
  output: 'standalone',
};

module.exports = nextConfig;
